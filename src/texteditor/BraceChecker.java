package texteditor;
import java.util.ArrayList;

public class BraceChecker {
	public static void main(String[] args) {
 String braces = "(){}[]";
 isValid(braces); // true
 isPossible;
		}
	 static boolean isValid(String braces) {
		    //store array of complete parans check length if its same as string len return true - for loop through string deleting pairs as you go (matching pairs)
		    String[] result;
		    int matchesFound = 0;
		    StringBuilder brace = new StringBuilder(braces);
		    ArrayList<String> foundBraces = new ArrayList<String>(); // Create an ArrayList object
		    StringBuilder braceMatched = new StringBuilder();
		    int leftB = 0;
		    int rightB = 0;
		    int leftP = 0;
		    int rightP =0;
		    int leftS = 0;
		    int rightS = 0;
		    String temp = "";
	        String gfg = new String(braces); 
System.out.print(gfg);
		    //while (brace.length() > 0) {
		   
		    
			return false;
			
		}
	 static void searcher(String[] brace) {
			for(int j = 1; j<brace.length; j++) {
				  
    		}
	 }

}